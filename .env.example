# NATS Home Automation Environment Variables

# Timezone configuration
TZ=America/New_York

# Container tool (docker or podman)
CONTAINER_TOOL=docker

# NATS Configuration
NATS_URL=nats://localhost:4222
NATS_USER=home
NATS_PASSWORD=changeme

# Service Ports
MANAGEMENT_UI_PORT=8081
HEALTH_MONITOR_PORT=8082
HOMEASSISTANT_PORT=8123
ZIGBEE2MQTT_PORT=8080

# Zigbee Adapter Device (update with your device)
ZIGBEE_DEVICE=/dev/ttyACM0

# MQTT Configuration
MQTT_BROKER_HOST=mosquitto
MQTT_BROKER_PORT=1883

# Container Images (optional - use GHCR images instead of building locally)
# Uncomment to use pre-built images from GitHub Container Registry
# GITHUB_REPOSITORY=calmera/nats-home-automation
# IMAGE_TAG=latest
# DISCOVERY_IMAGE=ghcr.io/${GITHUB_REPOSITORY}/discovery:${IMAGE_TAG}
# MANAGEMENT_UI_IMAGE=ghcr.io/${GITHUB_REPOSITORY}/management-ui:${IMAGE_TAG}
# HEALTH_MONITOR_IMAGE=ghcr.io/${GITHUB_REPOSITORY}/health-monitor:${IMAGE_TAG}

# Home Assistant Configuration
HOMEASSISTANT_CONFIG_DIR=./homeassistant-config