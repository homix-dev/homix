# Management UI Configuration

nats:
  url: nats://home:changeme@localhost:4222
  # creds: /path/to/nats.creds  # Optional, if using credentials file

http:
  addr: :8090
  static: ./static

api:
  prefix: /api/v1
  enable_cors: true

session:
  # secret: your-secret-key  # Optional, will be auto-generated if not set
  timeout: 3600s

# Enable debug logging
debug: false